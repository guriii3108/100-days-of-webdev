<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Day 78</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              slateglass: "rgba(30, 41, 59, 0.65)",
            },
          },
        },
      };
    </script>
  </head>
  <body
    class="min-h-screen bg-gradient-to-br from-slate-900 via-slate-950 to-slate-900 text-slate-100 flex items-center justify-center px-4"
  >
    <main
      class="relative w-full max-w-md rounded-2xl border border-slate-800 bg-slateglass backdrop-blur shadow-2xl shadow-blue-900/30"
    >
      <form action="/logout" method="POST" class="absolute right-4 top-4">
        <button
          type="submit"
          class="rounded-xl bg-gradient-to-r from-blue-500 via-indigo-500 to-purple-500 px-4 py-2 text-xs font-semibold uppercase tracking-wide text-white shadow-lg shadow-indigo-600/30 transition hover:shadow-indigo-600/50 focus:outline-none focus:ring-4 focus:ring-indigo-500/40 active:scale-[0.99]"
        >
          Logout
        </button>
      </form>

      <div class="p-8 space-y-6">
        <header class="space-y-2">
          <p
            class="text-xs font-semibold uppercase tracking-[0.25rem] text-blue-300/80"
          >
            Day 78
          </p>
          <h1 class="text-2xl font-bold text-white">Your Profile</h1>
          <p class="text-sm text-slate-300">Welcome back... !</p>
        </header>
      </div>
    </main>
  </body>
</html>

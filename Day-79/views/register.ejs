<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Day 79</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              slateglass: "rgba(30, 41, 59, 0.65)",
            },
          },
        },
      };
    </script>
  </head>
  <body
    class="min-h-screen bg-gradient-to-br from-slate-900 via-slate-950 to-slate-900 text-slate-100 flex items-center justify-center px-4"
  >
    <main
      class="w-full max-w-md rounded-2xl border border-slate-800 bg-slateglass backdrop-blur shadow-2xl shadow-blue-900/30"
    >
      <div class="p-8 space-y-8">
        <header class="space-y-2">
          <p
            class="text-xs font-semibold uppercase tracking-[0.25rem] text-blue-300/80"
          >
            Day 79
          </p>
          <h1 class="text-2xl font-bold text-white">Create your account</h1>
        </header>

        <form
          action="/register"
          method="POST"
          autocomplete="off"
          class="space-y-5"
        >
          <div class="space-y-2">
            <label for="name" class="block text-sm font-semibold text-slate-100"
              >Full name</label
            >
            <input
              id="name"
              name="name"
              type="text"
              placeholder="Jane Doe"
              required
              class="w-full rounded-xl border border-slate-700 bg-slate-900/60 px-3 py-2.5 text-slate-100 placeholder:text-slate-500 shadow-inner focus:border-blue-400 focus:outline-none focus:ring-2 focus:ring-blue-500/40 transition"
            />
          </div>

          <div class="space-y-2">
            <label
              for="userName"
              class="block text-sm font-semibold text-slate-100"
              >Username</label
            >
            <input
              id="userName"
              name="userName"
              type="text"
              placeholder="guriii3108"
              required
              class="w-full rounded-xl border border-slate-700 bg-slate-900/60 px-3 py-2.5 text-slate-100 placeholder:text-slate-500 shadow-inner focus:border-blue-400 focus:outline-none focus:ring-2 focus:ring-blue-500/40 transition"
            />
          </div>

          <div class="space-y-2">
            <div class="flex items-center justify-between">
              <label
                for="password"
                class="block text-sm font-semibold text-slate-100"
                >Password</label
              >
              <span class="text-xs text-slate-400">Min length: 4</span>
            </div>
            <input
              id="password"
              name="password"
              type="password"
              minlength="6"
              placeholder="******"
              required
              class="w-full rounded-xl border border-slate-700 bg-slate-900/60 px-3 py-2.5 text-slate-100 placeholder:text-slate-500 shadow-inner focus:border-blue-400 focus:outline-none focus:ring-2 focus:ring-blue-500/40 transition"
            />
          </div>

          <button
            type="submit"
            class="w-full rounded-xl bg-gradient-to-r from-blue-500 via-indigo-500 to-purple-500 px-4 py-3 text-sm font-semibold uppercase tracking-wide text-white shadow-lg shadow-indigo-600/30 transition hover:shadow-indigo-600/50 focus:outline-none focus:ring-4 focus:ring-indigo-500/40 active:scale-[0.99]"
          >
            Sign Up
          </button>
        </form>
      </div>
    </main>
  </body>
</html>
